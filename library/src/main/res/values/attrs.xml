<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="RefreshLayout">
        <attr name="refreshEnable" format="boolean" />
        <attr name="loadMoreEnable" format="boolean" />
        <attr name="pageCount" format="integer" />
        <attr name="showFloatView" format="boolean" />
        <attr name="failureLayout" format="reference" />
        <attr name="noDataLayout" format="reference" />
        <attr name="floatView" format="reference" />
        <attr name="column" format="integer" />
        <attr name="recyclerViewLayoutManager">
            <enum name="LinearLayoutManager" value="0" />
            <enum name="GridLayoutManager" value="1" />
            <enum name="StaggeredGridLayoutManager" value="2" />
        </attr>
    </declare-styleable>
</resources>
